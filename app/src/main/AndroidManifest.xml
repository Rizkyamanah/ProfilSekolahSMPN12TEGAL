<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.profilsekolahsmpn12tegal">

    <application
        android:allowBackup="true"
        android:icon="@drawable/remove1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProfilSekolahSMPN12TEGAL">
        <activity android:name=".SilatActivity"></activity>
        <activity android:name=".BadmintonActivity" />
        <activity android:name=".VollyActivity" />
        <activity android:name=".FutsalActivity" />
        <activity android:name=".VisimisiActivity" />
        <activity android:name=".EkstrakulikulerActivity" />
        <activity android:name=".AgendaActivity" />
        <activity android:name=".InformasiActivity" />
        <activity android:name=".PrestasiActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>